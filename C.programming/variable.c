//variable is the name of the memory location which stpres some data.
#include<stdio.h>

int main(){
    char star = '*';
    int number = 25;
    float pi = 3.14;
    int final_price = 100;
    printf("%d",final_price);
    }

    //variables are case sensitive
    //1st character is alphabet or '_'
    //no comma or blankspace
    //no other symbol other than '_'
